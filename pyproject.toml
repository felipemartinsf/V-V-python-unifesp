[project]
name = "v-v-python-unifesp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mutmut>=3.6.0",
    "pytest>=9.1.1",
    "pytest-sugar>=1.1.1",
]
